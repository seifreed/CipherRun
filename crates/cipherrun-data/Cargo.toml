[package]
name = "cipherrun-data"
version = "0.4.0"
edition = "2024"
rust-version = "1.88"
description = "Dependency-light data contracts for CipherRun rule packs"
license = "GPL-3.0-or-later"
repository = "https://github.com/seifreed/cipherrun"
readme = "README.md"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
